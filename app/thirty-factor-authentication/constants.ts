export const devMode = false
export const forceLevel = 0
export const maxLevel = 30
export const fallbackPassKey = 'fallback-password'
/** Width below which layouts stack (phones). */
export const mobileWidthBreakpoint = 550
/** Height below which chrome compacts and props shrink (short laptops, older phones). */
export const mobileHeightBreakpoint = 800
/** Viewport height at which fixed-size play areas render at 1:1. */
export const fitReferenceHeight = 900
/** Smallest scale fixed-size play areas will shrink to. */
export const minFit = 0.6
